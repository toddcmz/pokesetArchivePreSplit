{% extends 'base.jinja' %}

{% block content %}
    
     <h4>Add game score</h4>
    <form action="", method ="post">    
        {{ form.hidden_tag() }}
        <p>
            {{ form.sets_found.label}}<br>
            {{ form.sets_found(size=32)}}
        </p>
        <p>
            {{ form.submit() }}
        </p>
    </form>
    
{% endblock %}